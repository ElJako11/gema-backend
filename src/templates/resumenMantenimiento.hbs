<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            padding: 40px;
            color: #333;
        }

        /* Encabezado */
        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 30px;
            border-bottom: 2px solid #0056b3;
            padding-bottom: 15px;
        }

        .title-box h1 {
            margin: 0;
            font-size: 24px;
            text-transform: uppercase;
        }

        .title-box p {
            margin: 5px 0 0 0;
            color: #666;
            font-size: 14px;
        }

        /* Contenedor de Tarjetas */
        .cards-container {
            display: flex;
            flex-direction: column;
            gap: 15px;
        }

        .card {
            border: 1px solid #e0e0e0;
            border-radius: 8px;
            padding: 20px;
            background-color: #f9f9f9;
            page-break-inside: avoid;
            /* Evita que se corten al imprimir */
            position: relative;
        }

        .card-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 10px;
        }

        .job-title {
            font-size: 18px;
            font-weight: bold;
            color: #000;
        }

        /* Badges de estado */
        .badge {
            padding: 5px 12px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        /* Colores basados en tu imagen */
        .badge.no-empezado {
            background-color: #e2e3e5;
            color: #383d41;
        }

        .badge.en-ejecucion {
            background-color: #cce5ff;
            color: #004085;
        }

        /* Azul */
        .badge.reprogramado {
            background-color: #fff3cd;
            color: #856404;
        }

        /* Amarillo */
        .badge.culminado {
            background-color: #d4edda;
            color: #155724;
        }

        /* Verde */

        .info-row {
            display: flex;
            flex-direction: column;
            gap: 5px;
            font-size: 13px;
            color: #555;
        }

        .icon {
            margin-right: 6px;
        }
    </style>
</head>

<body>

    <div class="header">
        <div class="title-box">
            <h1>Resumen de Mantenimientos</h1>
            <p>{{tituloRango}}</p>
        </div>
        <div style="text-align: right; font-size: 12px; color: #888;">
            Fecha de reporte:<br>
            <strong>{{fechaGeneracion}}</strong>
        </div>
    </div>

    <div class="cards-container">
        {{#each mantenimientos}}
        <div class="card">
            <div class="card-header">
                <span class="job-title">{{nombreTrabajo}}</span>
                <span class="badge {{claseEstado}}">{{estado}}</span>
            </div>

            <div class="info-row">
                <div>
                    <span class="icon">📍</span>
                    <strong>Ubicación:</strong> {{ubicacion}}
                </div>
                <div>
                    <span class="icon">📅</span>
                    <strong>Fecha Límite:</strong> {{fechaLimiteFormateada}}
                </div>
            </div>
        </div>
        {{/each}}
    </div>

    {{#if inspecciones}}
    <div class="header" style="margin-top: 40px; border-bottom: 2px solid #28a745;">
        <div class="title-box">
            <h1>Resumen de Inspecciones</h1>
        </div>
    </div>

    <div class="cards-container">
        {{#each inspecciones}}
        <div class="card">
            <div class="card-header">
                <span class="job-title">{{nombreTrabajo}}</span>
                <span class="badge {{claseEstado}}">{{estado}}</span>
            </div>

            <div class="info-row">
                <div>
                    <span class="icon">📍</span>
                    <strong>Ubicación:</strong> {{ubicacion}}
                </div>
                <div>
                    <span class="icon">📅</span>
                    <strong>Fecha Creación:</strong> {{fechaCreacionFormateada}}
                </div>
            </div>
        </div>
        {{/each}}
    </div>
    {{/if}}

</body>

</html>